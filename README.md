# encode-club-weekend-project-4-db

db for tokensized ballot dApp

## steps to run `db`

- `build` container from Dockerfile right-click options
- `run` container form VSCode docker plugin area right-click options
